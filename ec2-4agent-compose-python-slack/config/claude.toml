${PLATFORM_BLOCK_CLAUDE}

[agent]
command = "claude-agent-acp"
args = []
working_dir = "/home/node"
env = { ANTHROPIC_API_KEY = "${ANTHROPIC_API_KEY}" }

[pool]
max_sessions = 10
session_ttl_hours = 24

[reactions]
enabled = true
remove_after_reply = false
